<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Steam2Browser</RootNamespace>
    <AssemblyName>steam2browser</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
    <GenerateEmbeddedFilesManifest>true</GenerateEmbeddedFilesManifest>
    <PublishSingleFile>true</PublishSingleFile>
    <!--
      No RuntimeIdentifier here on purpose: hardcoding one (e.g. win-x64) makes `dotnet build` /
      `dotnet run` produce a Windows apphost that cannot execute on Linux. Pass the target platform
      at publish time instead (`-r win-x64` or `-r linux-x64`, see README), and list both below so
      restore can resolve either without extra flags during plain `dotnet build`/`dotnet run`.
    -->
    <RuntimeIdentifiers>win-x64;linux-x64</RuntimeIdentifiers>
    <ServerGarbageCollection>true</ServerGarbageCollection>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.FileProviders.Embedded" Version="10.0.0" />
    <PackageReference Include="MonoTorrent" Version="3.0.2" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="wwwroot\**\*" />
    <EmbeddedResource Include="depotkeys.txt" LogicalName="Steam2Browser.depotkeys.txt" />

    <!--
      Optional catalog snapshot, produced by the build-index command (see README). When present the
      app starts with no network at all; without it, it falls back to fetching the index from a
      mirror. Not committed, so a plain clone still builds.
    -->
    <EmbeddedResource Include="index.bin" Condition="Exists('index.bin')" LogicalName="Steam2Browser.index.bin" />
  </ItemGroup>

  <!--
    The upstream repo has no tags or releases, so "is there a newer version" is answered by
    comparing the newest commit time on GitHub against when this binary was built.
  -->
  <ItemGroup>
    <AssemblyMetadata Include="BuildTimeUtc" Value="$([System.DateTime]::UtcNow.ToString('O'))" />
    <AssemblyMetadata Include="UpdateRepo" Value="extremebleem/steam2_downloader" />
  </ItemGroup>

</Project>
